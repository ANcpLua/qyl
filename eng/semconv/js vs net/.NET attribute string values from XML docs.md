# Extract .NET attribute string values from XML docs
      grep 'member name="F:' /tmp/otel-semconv-compare/dotnet-pkg/extracted/lib/netstandard2.0/OpenTelemetry.SemanticConventions.xml | sed 's/.*name="F:\(.*\)".*/\1/' | while read member; do
        # Get the field name from the member
        field=$(echo "$member" | sed 's/.*\.//')
        # The actual string value is the snake_case attribute name - extract from doc content
        true
      done

      # Better approach: extract from the XML summary blocks which reference the actual attribute names
      python3 -c "
      import xml.etree.ElementTree as ET
      tree = ET.parse('/tmp/otel-semconv-compare/dotnet-pkg/extracted/lib/netstandard2.0/OpenTelemetry.SemanticConventions.xml')
      members = tree.findall('.//member')
      # The .NET package uses field names like AttributeHttpMethod which map to 'http.method'
      # But we need the actual string values. Let's check the nuspec or source
      print('Classes:', len([m for m in members if m.get('name','').startswith('T:')]))
      print('Fields:', len([m for m in members if m.get('name','').startswith('F:')]))

      # Group by class
      from collections import defaultdict
      by_class = defaultdict(list)
      for m in members:
          name = m.get('name','')
          if name.startswith('F:'):
              parts = name[2:].rsplit('.', 1)
              if len(parts) == 2:
                  by_class[parts[0]].append(parts[1])

      for cls, fields in sorted(by_class.items()):
          print(f'\n{cls}: {len(fields)} fields')
          for f in fields[:5]:
              print(f'  {f}')
          if len(fields) > 5:
              print(f'  ... +{len(fields)-5} more')
      ")