{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git push*)",
            "command": "( cd /Users/ancplua/qyl && ./eng/build.sh Smoke 1>&2 ) || { echo '[BLOCK] PRD #173 smoke gate failed — fix the eng/smoke/run.sh output above and retry the push.' >&2; exit 2; }",
            "timeout": 600,
            "statusMessage": "Running PRD #173 smoke gate (nuke Smoke)…"
          }
        ]
      }
    ]
  }
}