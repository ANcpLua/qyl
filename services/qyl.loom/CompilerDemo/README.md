# Compiler demo slice

This folder is an isolated Loom prototype input for the new compiler surface.

It exists to feed the generator with live attributed examples:

- typed contracts
- one real tool
- workflow step executors
- a top-level detect -> plan -> fix -> verify -> report -> close workflow factory

This is intentionally not production glue.
