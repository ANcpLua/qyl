interface:
  display_name: "Using Git Worktrees"
  short_description: "Use when starting feature work that needs isolation from curr..."
  default_prompt: "Use $using-git-worktrees to use when starting feature work that needs isolation from curr."
