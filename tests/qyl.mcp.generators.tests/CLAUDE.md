@import ../../CLAUDE.md

# qyl.mcp.generators.tests

**Type:** Test Project  
**Framework:** net10.0  
**Language:** C# latest  
**SDK:** ANcpLua.NET.Sdk

## Test Configuration

- xUnit v3 with Microsoft Testing Platform
- AwesomeAssertions


## Banned APIs

- Use `TimeProvider` instead of current-time APIs
- Use `Lock` instead of `object` for locking
- Use `System.Text.Json` instead of Newtonsoft
