# Build outputs (UseArtifactsOutput centralizes to artifacts/)
artifacts/
!src/**/Artifacts/
# Loom/Claude agent worktrees (`git worktree add .agents/worktrees/<slug> …`); the
# worktree directory itself isn't checked in — only the trees it spawns live there.
.agents/worktrees/
[Bb]in/
[Oo]bj/
[Oo]ut/
[Ll]og/
[Ll]ogs/
!src/**/Logs/
*.dll
*.exe
*.pdb

# NuGet
*.nupkg
*.snupkg
*.nuget.props
*.nuget.targets
project.lock.json

# .NET
*.user
*.rsuser
*.suo
*.cache
*.userosscache
*.sln.docstates
**/Properties/launchSettings.json

# Rider/JetBrains
.idea/
*.sln.iml
*.DotSettings.user

# Visual Studio
.vs/
*.vspscc
*.vssscc
.builds
*.pidb
*.svclog
*.scc

# Local tool configuration. Repository-wide instructions live in AGENTS.md.
.claude/

# Plugin session state (Hades smart ledger, elegance pipeline, etc.)
.smart/

# Git worktrees (canonical dev worktrees under .worktrees/)
.worktrees/

# Playwright MCP logs
.playwright-mcp/

# Node.js / npm
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.npm/
.yarn/
*.tsbuildinfo

# Frontend build output
dist/
dist-extension/
.vite/

# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Nuke build temp / logs
.nuke/temp/
.nuke/build.schema.json

# qyl data files
*.duckdb
*.duckdb.wal
*.parquet
qyl.duckdb/
.qyl/binlogs/
.qyl/build-failures-spool.jsonl

# Environment and secrets
.env
.env.*
!.env.example
*.local
appsettings.*.json
!appsettings.Development.json.example

# Test results
TestResults/
test-results/
playwright-report/
coverage/
*.coverage
*.coveragexml

# Archives
*.zip
*.tar.gz
*.rar

# MSBuild debug dumps
**/preprocess.xml

# LSP config (local to each developer)
.lsp.json

# Python virtualenvs
**/venv/

# Misc
*.bak
*.swp
*.tmp
*~

# Per-tool instruction copies are forbidden. The only tracked CLAUDE.md is the
# root symlink to AGENTS.md.
**/CLAUDE.md
!CLAUDE.md
.mcp.json
tmp/
.tmp/

# Q&A session documents
QA*.md

# Research screenshots
images/
*.png
/eng/Benchmark.Large/Benchmark.cs
/eng/Benchmark.Large/Benchmark.Large.csproj
/eng/Benchmark.Large/Program.cs
/eng/Benchmark.Large/Types.cs
/docs/api-docs/

# Local agent scratchpads (objective.json etc.) — not repo content
.blackboard/
