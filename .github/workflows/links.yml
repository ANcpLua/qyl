# =============================================================================
# Link check (lychee)
# -----------------------------------------------------------------------------
# Fails when a markdown file references a local path that does not exist, a
# #fragment that does not resolve, or an external URL that is dead — README
# rot fails the build instead of accumulating. Config: lychee.toml. The
# weekly cron catches external links that die without a commit.
# =============================================================================

name: Links

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: "17 6 * * 1" # Mondays 06:17 UTC — external rot check without a commit
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: links-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  lychee:
    name: Link check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7

      - name: Check links
        uses: lycheeverse/lychee-action@e7477775783ea5526144ba13e8db5eec57747ce8 # v2.9.0
        with:
          args: --no-progress --root-dir "${{ github.workspace }}" .
          fail: true
        env:
          # Authenticated github.com link checks avoid anonymous rate limits.
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
